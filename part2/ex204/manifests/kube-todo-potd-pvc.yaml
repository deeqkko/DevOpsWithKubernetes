apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kube-todo-potd-pvc
  namespace: project
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
